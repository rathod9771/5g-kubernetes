# MACRO cell (defaults). Small cell overrides via values-small.yaml.
cellName: hcran-oai-macro
cellTier: macro
cell:
  gnbId: "0xe00"
  nrCellId: "12345678"
  physCellId: 0

# Bump by 1 whenever gnb.yaml template text changes directly (not via values)
configRevision: 1
