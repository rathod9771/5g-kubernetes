apiVersion: v1
kind: Service
metadata:
  name: amf-ngap-stable
  namespace: {{ .Release.Namespace | quote }}
spec:
  type: ClusterIP
  ports:
    - name: ngap
      port: 38412
      targetPort: ngap
      protocol: SCTP
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/name: amf
